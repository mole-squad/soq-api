setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
  - plan: scheduler
    as: SCHEDULER

  config:
    NOTIFICATIONS_ENABLED: true

build:
  docker:
    web: Dockerfile

run:
  web:
    command: bin/soq api
    image: web
