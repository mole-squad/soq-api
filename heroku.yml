setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE

build:
  docker:
    web: Dockerfile
run:
  web: bin/taskapp api
release:
  seed:
    command: bin/taskapp seed
    image: web
    env:
      TEST_USER_PASSWORD: 1234password
